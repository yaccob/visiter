#!/usr/bin/env viter
# Tight 2-hop crop around node 1. Ghost stubs show where edges
# leave the rendered region.
(viter(range(1, 30))
 .case(lambda x: x % 3 == 0, lambda x: x // 3)
 .default(lambda x: x + 2)
 .build()
 .to_dot(anchor=1, radius=2, direction="backward")
 .render())
