[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "visiter_native"
version = "0.0.0"
description = "Optional native BFS engine for visiter."
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
